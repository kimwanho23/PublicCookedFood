/**
 * User-authored recipe feature.
 *
 * <p>This module owns its own persistence model and only reuses the existing
 * comment/review mechanisms as implementation patterns.</p>
 */
package kwh.PublicCookedFood.userrecipe;
